# Local Environment Notes

Use these notes for Mona's Windows workstation.

## Python

Python is installed at:

```text
C:\Users\mona\AppData\Local\Programs\Python\Python313\python.exe
```

The user-level PATH should include:

```text
C:\Users\mona\AppData\Local\Programs\Python\Python313
C:\Users\mona\AppData\Local\Programs\Python\Python313\Scripts
```

If `python`, `python3`, or `py` are not found in an existing Codex shell process, call the absolute executable path above or prepend the Python directory to `$env:PATH` for that command.
